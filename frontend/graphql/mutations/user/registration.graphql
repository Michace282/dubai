mutation registration($email: String!, $password: String!, $passwordRepeat: String!, $guestUuid: UUID) {
    registration(
        input: {
            email: $email
            password: $password
            passwordRepeat: $passwordRepeat
            guestUuid: $guestUuid
        }
    ) {
        errors {
            field
            messages
        }
        user {
            id
            username
            firstName
            lastName
            email
        }
    }
}
